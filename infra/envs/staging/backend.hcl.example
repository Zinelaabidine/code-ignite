# Copy to backend.hcl (gitignored) and set the state bucket name.
#
#   terraform init -backend-config=backend.hcl
#
# This value MUST equal `terraform_state_bucket` in terraform.tfvars.
# scripts/pre-commit-check.sh asserts that they match.

bucket = "CHANGE-ME-terraform-state"
