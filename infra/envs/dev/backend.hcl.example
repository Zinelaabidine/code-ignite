# State bucket name is not stored here. At init, pass:
#
#   terraform init -backend-config="bucket=$(../../scripts/state-bucket-name.sh)"
#
# (from this env root, with AWS credentials). Bucket is derived from project_name
# in infra/common.tfvars and the current AWS account.
